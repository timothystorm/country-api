name: Test and Build
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: 🧾 Checkout source code
        uses: actions/checkout@v4

      - name: ☕ Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🛠️ Grant permissions
        run: chmod +x ./gradlew

      - name: 📦 Test and build
        run: ./gradlew clean build --console=auto -Dorg.gradle.logging.level=warn